# Vision Services


# Installation
Note: requirements are a bit excessive for now. We should clean it later if some of these packages are not used.

```
uv venv
source ./venv/bin/activate
uv pip install -r requirements.txt
python app.py
```

# Docker

